{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@heroicons/react": "^2.1.3",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.10",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.18.96",
    "@types/react": "^18.2.75",
    "@types/react-dom": "^18.2.24",
    "@types/react-router-dom": "^5.3.3",
    "autoprefixer": "^10.4.19",
    "axios": "^1.6.8",
    "heroicons": "^2.1.3",
    "lucide-react": "^0.368.0",
    "postcss": "^8.4.38",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3",
    "react-scripts": "5.0.1",
    "tailwindcss": "^3.4.3",
    "typescript": "^4.9.5",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:prod": "CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "populate-db": "node -e \"console.log('🚀 Executando população do banco de dados...'); require('./src/scripts/populateDatabase.ts').populateDatabase().then(() => console.log('✅ Concluído!')).catch(console.error)\"",
    "populate-subject": "node -e \"const subject = process.argv[2] || 'Direito Civil'; const count = process.argv[3] || 500; require('./src/scripts/populateDatabase.ts').populateSubjectContent(subject, count).then(() => console.log('✅ Concluído!')).catch(console.error)\"",
    "clear-db": "node -e \"console.log('🗑️ Limpando banco de dados...'); require('./src/scripts/populateDatabase.ts').clearDatabase().then(() => console.log('✅ Banco limpo!')).catch(console.error)\"",
    "backup-db": "node -e \"console.log('💾 Criando backup...'); require('./src/scripts/populateDatabase.ts').backupDatabase().then(() => console.log('✅ Backup criado!')).catch(console.error)\"",
    "generate-content": "node -e \"console.log('📝 Gerando conteúdo...'); const { generateAllContent } = require('./src/data/contentGenerator.ts'); const content = generateAllContent(); console.log('✅ Gerado:', content)\"",
    "content-stats": "node -e \"const { getContentStatistics } = require('./src/data/massiveContent.ts'); console.log('📊 Estatísticas:', getContentStatistics())\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
